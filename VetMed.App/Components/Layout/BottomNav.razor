@inject NavigationManager Nav

<nav class="bottom-nav">
    <NavLink href="/" Match="NavLinkMatch.All" class="nav-tab" ActiveClass="active">
        <span class="tab-icon">&#x1F3E0;</span>
        <span class="tab-label">Dom</span>
    </NavLink>
    <NavLink href="/book-visit" class="nav-tab" ActiveClass="active">
        <span class="tab-icon">&#x1F4C5;</span>
        <span class="tab-label">Wizyta</span>
    </NavLink>
    <NavLink href="/pets" class="nav-tab" ActiveClass="active">
        <span class="tab-icon">&#x1F43E;</span>
        <span class="tab-label">Zwierzaki</span>
    </NavLink>
    <NavLink href="/history" class="nav-tab" ActiveClass="active">
        <span class="tab-icon">&#x1F4CB;</span>
        <span class="tab-label">Historia</span>
    </NavLink>
</nav>

<style>
    .bottom-nav {
        position: fixed;
        bottom: 0;
        left: 50%;
        transform: translateX(-50%);
        width: 100%;
        max-width: 430px;
        background: #0D1F1C;
        border-top: 1px solid rgba(111,207,176,0.2);
        display: flex;
        justify-content: space-around;
        padding: 8px 0 20px;
        z-index: 100;
    }

    .nav-tab {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 2px;
        text-decoration: none;
        color: rgba(232,245,242,0.45);
        flex: 1;
        transition: color 0.2s;
    }

    .nav-tab.active {
        color: #6FCFB0;
    }

    .tab-icon {
        font-size: 22px;
        line-height: 1;
    }

    .tab-label {
        font-size: 10px;
        font-weight: 600;
        letter-spacing: 0.3px;
    }
</style>
